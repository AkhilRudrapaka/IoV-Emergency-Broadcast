[pytest]
; Project modules (vehicle.py, trust.py, clustering.py, ...) live at the repo
; root rather than inside a package, so the root must be on sys.path for the
; suite under tests/ to import them.
pythonpath = .
testpaths = tests
